# Production origin used for canonical URLs, Open Graph, the sitemap and robots.
# Without it those absolute URLs are omitted rather than pointing at localhost.
# Set the same value in the Vercel project environment.
NEXT_PUBLIC_SITE_URL=https://hammadahmad.dev
